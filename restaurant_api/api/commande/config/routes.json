{
  "routes": [
    {
      "method": "GET",
      "path": "/commande",
      "handler": "commande.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/commande/count",
      "handler": "commande.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/commande/:id",
      "handler": "commande.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/commande",
      "handler": "commande.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/commande/:id",
      "handler": "commande.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/commande/:id",
      "handler": "commande.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
